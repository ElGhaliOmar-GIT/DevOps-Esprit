name: Trigger Jenkins Pipeline

on:
  push:
    branches:
      - siwardhrif

jobs:
  trigger-jenkins:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Jenkins
        run: |
          curl -X POST -k http://192.168.100.122:8080/job/ProjectDevops?token=11dfd311abda40cc6edcf255762fe49be7