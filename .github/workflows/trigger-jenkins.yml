name: Trigger Jenkins Pipeline

on:
  push:
    branches:
      - siwardhrif

jobs:
  trigger-jenkins:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Jenkins
        run: |
          curl -X POST -k http://192.168.100.122:8080/job/ProjectDevops/build?token=11155d676cb582ea40ecf0d53cd108a6d5