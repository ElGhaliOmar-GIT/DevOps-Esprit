name: Trigger Jenkins Pipeline

on:
  push:
    branches:
      - siwardhrif

jobs:
  trigger-jenkins:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Jenkins
        run: |
          curl -X POST -k http://192.168.100.122:8080/job/ProjectDevops/build?token=kL9$2jP!wQsRzT